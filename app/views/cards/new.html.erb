<h1>ビンゴゲーム</h1>
<% flash.each do |type, msg| %>
  <div>
    <%= msg %>
  </div>
<% end %>
<p>
  <%= form_tag({action: "create"}, method: "post") do %>
    <%= hidden_field_tag(:game_id, @game.id) %>
    <%= submit_tag("ゲームに参加する") %>
  <% end %>
</p>
